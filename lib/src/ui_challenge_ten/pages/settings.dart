import 'package:flutter/material.dart';

class SettingsTen extends StatelessWidget {
  const SettingsTen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text('Settings'),
    );
  }
}
