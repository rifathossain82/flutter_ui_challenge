import 'package:flutter/material.dart';

class CartTen extends StatelessWidget {
  const CartTen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text('Cart Screen'),
    );
  }
}
