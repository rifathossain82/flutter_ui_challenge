import 'package:flutter/material.dart';

class FavouriteTen extends StatelessWidget {
  const FavouriteTen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      alignment: Alignment.center,
      child: Text('Favourite'),
    );
  }
}
